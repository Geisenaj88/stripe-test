import React from 'react';
import Layout from './shared/layout';

const NotFound = () => {
    
    return (
        <Layout>
            <p style={{'fontWeight': 'bold','textAlign': 'center'}}>Unfortunately, we can't find that page.</p>
        </Layout>
    )
}

export default NotFound;